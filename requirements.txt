# Python 3.11 requirements for the DT-GPT MIMIC DoRA/Unsloth path.
#
# This file replaces the old Python 3.8 frozen environment. It intentionally
# keeps the install set focused on the current MIMIC fine-tuning/evaluation
# workflow instead of pinning every notebook, time-series, and TensorFlow
# package from the historical environment.
#
# Notes:
# - Install inside a conda env such as `dtgpt-unsloth`.
# - `flash-attn` is not included because it often requires a GPU/compiler
#   build path and is optional for this job.
# - `deepspeed` is not included because submit_mimic_dora.sh defaults to
#   DTGPT_USE_DEEPSPEED=0. Install it separately only if you enable DeepSpeed.
# - NVIDIA CUDA wheels are resolved through the selected torch wheel; do not
#   pin both cu11 and cu12 runtime packages here.

--extra-index-url https://download.pytorch.org/whl/cu121

# Core GPU stack used by the current l40s CUDA 12.1 setup.
torch==2.1.1
triton==2.1.0
sympy==1.14.0
mpmath==1.3.0
networkx>=3.1
filelock>=3.10.0

# Unsloth / QLoRA / DoRA training stack.
# Use the explicit torch/CUDA extra instead of bare `unsloth`; the bare package
# can spend a long time resolving GPU dependencies in this cluster environment.
unsloth[cu121-torch211] @ git+https://github.com/unslothai/unsloth.git
accelerate>=0.27.2
bitsandbytes>=0.43.0
datasets>=2.18.0,<3.0.0
peft>=0.8.2
transformers==4.46.3
trl==0.12.2
safetensors>=0.4.2
sentencepiece>=0.1.99
typer>=0.12.5,<1.0.0
click>=8.1.7
sniffio==1.3.1
anyio>=4.3.0
httpx>=0.27.0

# Numeric/data processing used by pipeline and MIMIC conversion/evaluation.
numpy>=1.26.0,<2.0.0
pandas>=2.1.0,<3.0.0
scipy>=1.11.0,<2.0.0
scikit-learn>=1.3.0,<2.0.0
pyarrow>=14.0.0

# Experiment tracking, metrics, plotting, and utilities.
evaluate>=0.4.0
GPUtil==1.4.0
matplotlib>=3.8.0
plotnine>=0.12.0
rouge-score>=0.1.2
seaborn>=0.13.0
wandb>=0.16.1

# Runtime helpers used by local smoke checks / exploration.
packaging>=23.2
protobuf>=4.25.0
psutil>=5.9.0
PyYAML>=6.0.1
regex>=2023.12.25
requests>=2.31.0
rich>=13.7.0
ruff>=0.3.1
tqdm>=4.66.0
